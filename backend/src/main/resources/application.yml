spring:
  datasource:
    url: jdbc:mysql://localhost:3306/java_database?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: deep@125
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

app:
  jwt:
    secret: ==========================PizzariyaSecretKeyForJWTAuth256Bits==========================
    expiration: 3600000
  google:
    clientId: YOUR_CLIENT_ID_HERE
